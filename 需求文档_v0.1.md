# Blueis - 非关系型缓存数据库 初期需求文档 v0.1

## 1. 项目概述

### 1.1 项目背景
仿照 Redis 设计并实现一个轻量级的非关系型（NoSQL）缓存数据库，命名为 **Blueis**。初期主要面向 Windows 平台，以学习和实践为目的，暂不追求极致的性能优化，聚焦于基本功能的完备性。

### 1.2 核心目标
- 提供 Redis 风格的内存数据存储能力
- 支持使用 **SQL 语句**进行数据的增删改查
- 提供 TCP 网络接口供客户端连接
- 具备基础的持久化能力

---

## 2. 技术选型建议

| 层面 | 推荐方案 | 理由 |
|------|---------|------|
| 编程语言 | **C++17** | Windows 原生，零运行时依赖，后续性能优化空间大 |
| 构建工具 | **CMake** | 跨平台构建，VS/MinGW 均可使用 |
| SQL 解析 | 手写简易递归下降解析器 | 初期只支持 DML 子集，无需完整 SQL 引擎 |
| 网络协议 | 自定义文本协议（类似 Redis RESP） | 简单易调试，也可后续兼容 Redis 客户端 |
| 序列化 | JSON（持久化）/ 自定义二进制 | 使用 [nlohmann/json](https://github.com/nlohmann/json) 头文件库 |
| 并发 | `std::thread` + `std::shared_mutex` | C++17 标准库原生支持 |

> 推荐使用 **C++20**，理由：直接使用 Windows Socket API (Winsock2)，`std::thread` / `std::shared_mutex` / `std::unordered_map` 等标准库组件即可满足初期需求，无需第三方依赖，后续性能优化空间充足。

---

## 3. 功能需求

### 3.1 数据模型（存储层）

内存中维护一个全局的 key-value 存储空间，支持以下数据结构（v0.1 优先实现前两种）：

| 结构 | 说明 | 对应 Redis 类型 |
|------|------|----------------|
| **String** | 键值对，值可以是字符串/数字/布尔 | string |
| **Hash** | 一个 key 对应多个 field-value 子字段 | hash |
| List | 有序字符串列表（v0.2 计划） | list |
| Set | 无序唯一集合（v0.2 计划） | set |

### 3.2 SQL 支持（v0.1）

将 SQL 语句映射到上述存储结构上执行。

#### 支持的 SQL 语句

```sql
-- 创建表（定义逻辑结构，底层使用 Hash 或 String 存储）
CREATE TABLE user (
    id        INT PRIMARY KEY,
    name      VARCHAR(64),
    age       INT,
    email     VARCHAR(128)
);

-- 插入数据
INSERT INTO user (id, name, age, email) VALUES (1, 'Alice', 30, 'alice@example.com');

-- 查询数据
SELECT * FROM user WHERE id = 1;
SELECT name, age FROM user WHERE age > 25;
SELECT * FROM user WHERE name LIKE '%li%';

-- 更新数据
UPDATE user SET age = 31 WHERE id = 1;

-- 删除数据
DELETE FROM user WHERE id = 1;

-- 删除表
DROP TABLE user;

-- 列出所有表
SHOW TABLES;
```

#### SQL 子句支持范围

| 子句 | 支持情况 |
|------|---------|
| `SELECT ... FROM ...` | 必须支持 |
| `WHERE` | 支持，含 `=`, `!=`, `>`, `<`, `>=`, `<=`, `LIKE`, `AND`, `OR` |
| `ORDER BY` | v0.1 可选 |
| `LIMIT / OFFSET` | v0.1 可选 |
| `INSERT INTO ... VALUES` | 必须支持 |
| `UPDATE ... SET ... WHERE` | 必须支持 |
| `DELETE FROM ... WHERE` | 必须支持 |
| `JOIN` | v0.1 不支持 |

#### SQL 到 NoSQL 的映射策略

```
CREATE TABLE user (...)       → 在元数据中注册 "user" 表结构
INSERT INTO user (...) VALUES (...) → 生成唯一 key: "user:{id}"，值存为 Hash
SELECT * FROM user WHERE ...  → 扫描所有 user:* 的 key，反序列化后过滤
UPDATE user SET ... WHERE ... → 扫描匹配，修改对应字段
DELETE FROM user WHERE ...    → 扫描匹配，删除对应 key
```

### 3.3 Redis 兼容命令（可选）

在 SQL 之外，额外支持部分 Redis 命令，方便习惯 Redis 协议的用户：

| 命令 | 说明 |
|------|------|
| `SET key value` | 设置字符串键值对 |
| `GET key` | 获取字符串值 |
| `DEL key` | 删除键 |
| `EXISTS key` | 检查键是否存在 |
| `HSET key field value` | 设置哈希字段 |
| `HGET key field` | 获取哈希字段 |
| `HGETALL key` | 获取所有哈希字段 |
| `KEYS pattern` | 匹配键名 |

### 3.4 网络接口

| 项目 | 说明 |
|------|------|
| 协议 | 文本协议，每行一条命令，`\r\n` 分割 |
| 默认端口 | **6380**（避免与本地 Redis 6379 冲突） |
| 并发 | 支持多客户端同时连接，每个连接独立会话 |
| 返回格式 | 查询结果返回 ASCII 表格 / JSON / 自定义格式 |

**交互示例（telnet / nc）：**
```
$ telnet 127.0.0.1 6380

> CREATE TABLE user (id INT PRIMARY KEY, name VARCHAR(64), age INT);
OK

> INSERT INTO user (id, name, age) VALUES (1, 'Alice', 30);
OK (1 row affected)

> SELECT * FROM user;
+----+-------+-----+
| id | name  | age |
+----+-------+-----+
| 1  | Alice | 30  |
+----+-------+-----+
1 row in set

> GET user:1
{"id":1,"name":"Alice","age":30}
```

### 3.5 持久化（v0.1 基础版本）

| 机制 | 说明 |
|------|------|
| **快照（Snapshot）** | 定时或手动将全部数据序列化为 JSON / 自定义格式保存到磁盘 |
| 保存路径 | 运行目录下的 `data/` 文件夹 |
| 启动恢复 | 启动时自动检测并加载快照文件 |
| 保存触发 | `SAVE` 命令 / 服务关闭时自动保存 |

---

## 4. 系统架构

```
┌─────────────────────────────────────────────────┐
│                   Client                        │
│    (telnet / nc / 自定义客户端 / Redis CLI)       │
└──────────────────┬──────────────────────────────┘
                   │ TCP / 自定义文本协议
                   ▼
┌─────────────────────────────────────────────────┐
│               Network Layer                      │
│          TCP Server (port 6380)                  │
│          Connection Manager                      │
│          Protocol Parser                         │
└──────────────────┬──────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────────┐
│               Command Router                     │
│          SQL Command → SQL Engine                │
│          Redis Command → NoSQL Engine            │
└──────┬──────────────────────────┬───────────────┘
       │                          │
       ▼                          ▼
┌──────────────┐      ┌──────────────────────────┐
│  SQL Engine  │      │    NoSQL Engine           │
│  (Parser +   │      │  (SET/GET/HSET/HGET/...) │
│   Executor)  │      │                          │
└──────┬───────┘      └────────┬─────────────────┘
       │                       │
       └──────────┬────────────┘
                  ▼
┌─────────────────────────────────────────────────┐
│              Storage Engine                      │
│         In-Memory Map (sync.RWMutex)             │
│         Key → Value (String / Hash / List / Set) │
│         Metadata Store (Table Schemas)            │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│             Persistence Layer                    │
│         Snapshot (JSON Serializer)               │
│         File I/O (data/blueis.db)                 │
└─────────────────────────────────────────────────┘
```

### 模块职责

| 模块 | 职责 |
|------|------|
| **Network Layer** | TCP 监听、连接管理、读取客户端请求、发送响应 |
| **Protocol Parser** | 解析文本命令，区分 SQL 语句和 Redis 命令 |
| **SQL Engine** | SQL 词法/语法分析、语义检查、生成执行计划 |
| **NoSQL Engine** | 执行 Redis 风格命令（SET/GET/HSET 等） |
| **Storage Engine** | 管理内存中的数据映射，提供线程安全的数据访问 |
| **Persistence Layer** | 定时/手动快照保存，启动时数据恢复 |

---

## 5. 数据结构设计（存储层核心）

```cpp
// 存储引擎核心数据结构

// 数据类型枚举
enum class DataType {
    String,  // 字符串
    Hash,    // 哈希
    List,    // 列表 (v0.2)
    Set,     // 集合 (v0.2)
    ZSet,    // 有序集合 (v0.2)
};

// 列定义
struct ColumnDef {
    std::string name;
    std::string type;       // INT, VARCHAR, BOOL, FLOAT
    int size = 0;
    bool is_primary = false;
};

// 表元数据
struct TableMeta {
    std::string name;
    std::vector<ColumnDef> columns;
    std::string primary_key; // 主键字段名
};

// 数据行 - 内部统一存储结构
using DataRow = std::unordered_map<std::string, std::any>;

// 存储引擎
class StorageEngine {
private:
    std::shared_mutex m_mutex;
    std::unordered_map<std::string, std::any> m_keys;          // key → value (String 类型)
    std::unordered_map<std::string, std::unordered_map<std::string, std::string>> m_hashes; // key → {field: value}
    std::unordered_map<std::string, TableMeta> m_tables;       // 表元数据
    std::unordered_map<std::string, std::unordered_map<std::string, DataRow>> m_rows; // 表名 → {rowKey: DataRow}
};
```

---

## 6. 开发路线图

### Phase 1 — 基础骨架（第 1-2 周）
- [ ] 项目初始化（CMake + C++20 工程搭建）
- [ ] TCP Server 基础框架（监听 + 连接管理）
- [ ] 文本协议解析器（读取命令 → token 化）
- [ ] Storage Engine：String 类型的 SET/GET/DEL/EXISTS

### Phase 2 — SQL 引擎（第 3-4 周）
- [ ] SQL 词法分析器（Tokenizer）
- [ ] SQL 语法分析器（Parser）：CREATE TABLE / INSERT / SELECT / UPDATE / DELETE
- [ ] SQL Executor：将 SQL 转换为存储引擎操作
- [ ] 结果集格式化：ASCII 表格输出

### Phase 3 — Hash 支持 + Redis 命令（第 5 周）
- [ ] Hash 类型的 HSET/HGET/HDEL/HGETALL
- [ ] Redis 兼容命令适配
- [ ] `LIKE` 子句支持
- [ ] 错误处理与异常反馈

### Phase 4 — 持久化（第 6 周）
- [ ] 快照保存（JSON 序列化所有数据）
- [ ] 启动时数据恢复
- [ ] `SAVE` / `LOAD` 命令
- [ ] 服务关闭时的自动保存

### Phase 5 — 完善与测试（第 7-8 周）
- [ ] 集成测试覆盖所有 SQL 场景
- [ ] 并发安全验证（竞态测试）
- [ ] 边界情况处理
- [ ] 基础文档与使用说明

---

## 7. 项目结构（推荐）

```
Blueis/
├── src/
│   ├── main.cpp             # 主入口
│   ├── server/
│   │   ├── server.cpp       # TCP 网络层
│   │   ├── server.h
│   │   └── connection.cpp   # 连接管理
│   │   └── connection.h
│   ├── protocol/
│   │   ├── parser.cpp       # 协议解析
│   │   └── parser.h
│   ├── sql/
│   │   ├── lexer.cpp        # 词法分析
│   │   ├── lexer.h
│   │   ├── parser.cpp       # 语法分析
│   │   ├── parser.h
│   │   ├── ast.h            # 抽象语法树定义
│   │   └── executor.cpp     # SQL 执行器
│   │   └── executor.h
│   ├── storage/
│   │   ├── engine.cpp       # 存储引擎
│   │   ├── engine.h
│   │   ├── string_store.cpp # String 类型操作
│   │   ├── string_store.h
│   │   ├── hash_store.cpp   # Hash 类型操作
│   │   └── hash_store.h
│   └── persistence/
│       ├── snapshot.cpp     # 持久化快照
│       └── snapshot.h
├── include/                 # 公共头文件
│   └── common.h             # 通用类型定义
├── tests/                   # 单元测试
├── CMakeLists.txt           # CMake 构建配置
├── .gitignore
└── README.md
```

---

## 8. 非功能需求

| 需求 | 说明 |
|------|------|
| 并发支持 | 使用读写锁 (`sync.RWMutex`) 保证并发安全 |
| 内存限制 | 初期不设上限，依赖系统可用内存 |
| 启动时间 | 目标 < 1 秒（空数据时）|
| 协议兼容 | 命令不区分大小写 |
| 编码 | 全部 UTF-8 |
| 错误处理 | 返回清晰的人类可读错误信息，不 panic |

---

## 9. 附录：交互示例场景

### 场景：用户管理系统

```
# 创建表
CREATE TABLE user (id INT PRIMARY KEY, name VARCHAR(64), age INT, city VARCHAR(32));

# 插入 3 条数据
INSERT INTO user (id, name, age, city) VALUES (1, '张三', 28, '北京');
INSERT INTO user (id, name, age, city) VALUES (2, '李四', 35, '上海');
INSERT INTO user (id, name, age, city) VALUES (3, '王五', 42, '深圳');

# 查询所有
SELECT * FROM user;

# 条件查询
SELECT name, age FROM user WHERE age > 30;

# 模糊查询
SELECT * FROM user WHERE name LIKE '%三%';

# 更新
UPDATE user SET city = '广州' WHERE id = 2;

# 删除
DELETE FROM user WHERE id = 3;

# 验证
SELECT * FROM user;
```

---

*本文档为 v0.1 初稿，后续根据开发进展持续迭代。*
