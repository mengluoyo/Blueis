# Blueis 代码学习指南

按照一条 SQL 语句的完整执行路径，从入口到出口逐步深入。

---

## Phase 1：数据基础（先看懂数据长什么样）

### 1.1 `include/common.h` — 全局类型定义

这是整个项目的"字典"，所有模块共享的类型都在这里。


| 类型                        | 用途                                  |
| ------------------------- | ----------------------------------- |
| `Command`                 | 一条解析后的命令（type + tokens + raw）       |
| `CommandType`             | SQL / Redis / Unknown 三分法           |
| `ColumnDef` / `TableMeta` | 表的列定义和元数据                           |
| `DataRow`                 | 一行数据 = `unordered_map<string, any>` |


学习要点：理解 `DataRow` —— 它的每个字段值是 `std::any`，这意味着同一行里不同的列可以存不同类型（int / string / double）。

### 1.2 `src/storage/engine.h` + `engine.cpp` — 存储引擎（单例）

这是整个数据库的"心脏"。打开 [engine.h](src/storage/engine.h) 看私有成员（第 63-67 行）：

```cpp
unordered_map<string, string>                 m_strings;  // key → value
unordered_map<string, TableMeta>              m_tables;   // 表名 → 表结构
unordered_map<string, unordered_map<...>>     m_rows;     // "表名:主键值" → 行数据
unordered_map<string, unordered_map<...>>     m_hashes;   // key → {field → value}
```

四个 map 就是全部数据。所有 String/Hash/Table 操作最终都是对这四个容器的增删改查。

**学习要点**：

- `shared_mutex` 的用法：读操作用 `shared_lock`（允许多个读者并发），写操作用 `unique_lock`（独占）
- `m_rows` 的 key 格式是 `"tableName:pkValue"`，这是模拟关系数据库的主键索引
- `row_value_str()` 函数展示了如何从 `std::any` 中提取不同类型的值

**阅读顺序**：先看 `set()` / `get()`（最简单），再看 `hset()` / `hget()`，最后看 `create_table()` / `insert_row()`

---

## Phase 2：协议层（命令是怎么被识别出来的）

### `src/protocol/command_parser.h` + `command_parser.cpp`

这个模块只做一件事：把原始字符串拆成 `Command`。

```
"SET mykey myvalue"  →  Command{type=Redis, tokens=["set","mykey","myvalue"]}
"SELECT * FROM t"    →  Command{type=SQL, raw="SELECT * FROM t"}
```

**学习要点**：

- `parse()` 方法：按空格和引号分词 → 第一个 token 转小写 → 判断是 SQL 关键字还是 Redis 命令
- SQL 命令保留了 `raw`（原始字符串），因为后续 Lexer 需要自己重新解析

---

## Phase 3：SQL 引擎（核心——字符串如何变成数据库操作）

这是整个项目最值得学深的部分。一条 SQL 的处理链路：

```
原始字符串 → [Lexer] → Token流 → [Parser] → AST → [Executor] → 存储操作 → [Formatter] → 输出
```

### 3.1 `src/sql/lexer.h` + `lexer.cpp` — 词法分析

把字符串切成一个个 Token。

```
"SELECT * FROM users WHERE id = 1"
    ↓
SELECT(*Keyword)  *(*Star)  FROM(*Keyword)  users(*Identifier)  WHERE(*Keyword)  id(*Identifier)  =(*Eq)  1(*Integer)
```

**学习要点**：

- `next()` 方法：跳过空白 → 看当前字符决定类型（字母→标识符/关键字，数字→整数，引号→字符串，符号→运算符）
- `match_kw()`：把标识符和关键字集合对比，决定它是 `Keyword` 还是 `Identifier`
- `peek()` 是 lookahead（预读一个 token 但不消费），Parser 会用到

### 3.2 `src/sql/ast.h` + `ast.cpp` — 抽象语法树

定义了所有 SQL 语句的结构体。

```
Expr（表达式）: 一棵二叉树
    ├── Ident   ("id")
    ├── Literal (1, "hello", 3.14)
    └── Binary  (op, left_child, right_child)

Stmt（语句）: std::variant<CreateTable, Insert, Select, Update, Delete, DropTable, ShowTables>
```

**学习要点**：

- `Expr` 用 `unique_ptr<Expr>` 构建二叉树——`WHERE id = 1 AND name LIKE '%a'` 会变成一棵嵌套的二叉表达式树
- `Stmt` 是 `std::variant`，8 种语句类型共享同一个类型名。这比虚函数继承更轻量（编译期多态）
- factory 方法（`Expr::binary()` 等）让 AST 构建更简洁

### 3.3 `src/sql/parser.h` + `parser.cpp` — 语法分析（递归下降）

把 Token 流变成 AST。这是学习难度最高的模块。

**核心思想——递归下降**：每种 SQL 子句对应一个 parse 方法，方法与语法规则一一对应。

```
parse() → 看第一个关键字 → 分派到具体方法
    ├── parse_select()   → SELECT 列 FROM 表 WHERE 条件
    ├── parse_insert()   → INSERT INTO 表 (列) VALUES (值)
    ├── parse_create()   → CREATE TABLE 表 (列定义, ...)
    ├── parse_update()   → UPDATE 表 SET 列=值 WHERE 条件
    ├── parse_delete()   → DELETE FROM 表 WHERE 条件
    ├── parse_drop()     → DROP TABLE 表
    └── parse_show()     → SHOW TABLES

parse_expr() → parse_or() → parse_and() → parse_compare()
    ↑ 运算符优先级从低到高 ↑
```

**学习要点**：

- 优先级通过函数调用链实现：`parse_expr` 调用 `parse_or`，`parse_or` 调用 `parse_and`，`parse_and` 调用 `parse_compare`。越外层优先级越低
- `parse_compare()` 先解析左操作数（标识符或字面量），再看下一个 token 是不是比较符（`=` `!=` `>` `<` 等），是的话构建 Binary 节点
- 用实际例子走读：`SELECT * FROM users WHERE age > 18 AND name = 'tom'`，在纸上画出调用栈和 AST 树

### 3.4 `src/sql/executor.h` + `executor.cpp` — 执行器

遍历 AST，调用 StorageEngine 完成实际操作。

```cpp
Result execute(const Stmt& stmt) {
    if (holds_alternative<SelectStmt>(stmt))  return exec_select(get<SelectStmt>(stmt));
    if (holds_alternative<InsertStmt>(stmt))  return exec_insert(get<InsertStmt>(stmt));
    // ...
}
```

**学习要点**：

- `std::holds_alternative<T>()` + `std::get<T>()` 是 `variant` 的标准访问模式
- `eval_expr()` 是 WHERE 条件求值的核心——递归遍历表达式二叉树
  - `Ident` → 从 DataRow 中取值
  - `Literal` → 直接返回值
  - `Binary` → 根据 op 比较左右子树的结果（支持 `=` `!=` `>` `<` `>=` `<=` `AND` `OR`）
- `exec_select()` 展示了一个典型的查询流程：取所有行 → 逐行过 WHERE → 构造结果集

### 3.5 `src/sql/formatter.h` + `formatter.cpp` — 结果格式化

把 `Executor::Result` 变成 ASCII 表格。

```
+----+-------+
| id | name  |
+----+-------+
| 1  | tom   |
+----+-------+
1 row in set
```

**学习要点**：列宽计算（取列名和所有数据中最大宽度）、分隔线构建（`+---+---+`）、右填充空格对齐。

---

## Phase 4：基础设施（服务器和持久化）

### 4.1 `src/server/server.h` + `server.cpp` — TCP 服务器

Windows Winsock2 实现的 TCP 服务器，多线程（每个客户端一个线程）。

**学习要点**：

- 主线程 `run()`：accept → 清理已完成线程 → 新建线程处理客户端
- `handle_client()`：逐字符处理输入（退格、方向键、Enter、普通字符）
- `filter_telnet()`：过滤 telnet IAC 协商序列（0xFF 开头）
- `process_command()`：协议分发 → SQL 或 Redis 引擎
- `redraw()`：终端行编辑重绘（CR + 清屏 + 输出 + 光标定位）

### 4.2 `src/persistence/json.h` + `json.cpp` — 自定义 JSON 解析器

不依赖第三方库，手写的递归下降 JSON 解析器。

**学习要点**：

- `Writer`：用 `ObjectScope` / `ArrayScope` RAII 类自动管理 `{ }` `[ ]` 和逗号
- `Reader`：`parse_value()` 看首字符分发（`{` → parse_object, `[` → parse_array, `"` → parse_string, 数字 → parse_number）
- `Value`：`variant<nullptr_t, bool, int64_t, double, string, vector<Value>, vector<pair<string, Value>>>` — JSON 的六种值类型

### 4.3 `src/persistence/snapshot.h` + `snapshot.cpp` — 快照持久化

连接 JSON 和 StorageEngine 的桥接层。

`save()` 流程：StorageEngine → 遍历所有数据 → JSON Writer → 写入文件
`load()` 流程：读取文件 → JSON Reader → 逐层解析 → 恢复到 StorageEngine

---

## 建议的学习路线图


| 天数    | 内容                                                  |
| ----- | --------------------------------------------------- |
| Day 1 | `common.h` + `engine.h/cpp`（理解数据模型和存储）              |
| Day 2 | `command_parser.h/cpp` + `lexer.h/cpp`（理解字符串→Token） |
| Day 3 | `ast.h/cpp` + `parser.h/cpp`（理解 Token→AST，**最核心**）  |
| Day 4 | `executor.h/cpp` + `formatter.h/cpp`（理解 AST→结果）     |
| Day 5 | `server.h/cpp` + `persistence/`（理解服务器和持久化）          |


**推荐的学习方式**：

1. 在每个 `.cpp` 文件里找一个最简单的函数，设断点，用 GDB 单步跟一遍
2. 用实际输入做"手工演算"：拿 `SELECT * FROM t WHERE a > 1` 在纸上写出 Lexer 输出、Parser 输出的 AST 树、Executor 如何遍历
3. 尝试自己改一个小功能（比如加一个 `COUNT(*)` 支持），会逼着你把整条链路走通

