# telnet 行编辑（readline）实现参考

## 1. 方向键的转义序列

telnet 客户端按下方向键时，不是发单个字节，而是发送 **ANSI 转义序列**：

| 按键 | 发送的字节 | 说明 |
|------|-----------|------|
| 左箭头 | `\x1b [ D` | ESC + [ + D |
| 右箭头 | `\x1b [ C` | ESC + [ + C |
| 上箭头 | `\x1b [ A` | ESC + [ + A |
| 下箭头 | `\x1b [ B` | ESC + [ + B |
| Home | `\x1b [ H` 或 `\x1b [ 1 ~` | 视终端而定 |
| End | `\x1b [ F` 或 `\x1b [ 4 ~` | 视终端而定 |
| Delete | `\x1b [ 3 ~` | 删除光标后字符 |

`\x1b` 就是 ESC 键的 ASCII 码（十进制 27），转义序列的固定开头。

## 2. 识别转义序列的策略

收到的数据是字节流，ESC 序列分散在多个 `recv` 或同一个 `recv` 中都有可能。标准做法：

```
维护一个 esc 状态, 以及 esc_buf（累积序列用）

收到 \x1b  → 进入 ESC 模式, 清空 esc_buf, 存入 \x1b
ESC 模式下:
  收到 [ 或 O   → 继续累积
  收到 A-Z / a-z / ~ → 序列结束, 查表匹配动作
  收到其他     → 不是转义序列, 退出 ESC 模式, 丢弃
```

另一种更简单的思路：**只处理连续到达的完整序列**。因为 telnet 是 TCP 字节流，ESC `[` 字母通常在一次 `recv` 中一起到达（但不是绝对保证）。

## 3. 光标位置追踪

在服务器端维护一个整数 `cursor`，表示光标在 `sql_buf` 中的位置：

- `cursor = 0` → 光标在行首
- `cursor = sql_buf.size()` → 光标在行尾（默认追加位置）

**插入字符**（普通打字）：
```
sql_buf.insert(cursor, 1, c);  // 在 cursor 位置插入
cursor++;
```

**退格**（`\b` 或 DEL `0x7F`）：
```
if (cursor > 0) {
    sql_buf.erase(cursor - 1, 1);  // 删光标左边那个字符
    cursor--;
}
```

**Delete 键**（`\x1b [ 3 ~`）：
```
if (cursor < sql_buf.size()) {
    sql_buf.erase(cursor, 1);  // 删光标处的字符, 不移动光标
}
```

**左箭头**：`cursor = max(0, cursor - 1)`
**右箭头**：`cursor = min(sql_buf.size(), cursor + 1)`
**Home**：`cursor = 0`
**End**：`cursor = sql_buf.size()`

## 4. 移动终端光标（ANSI 控制码）

编辑后需要让终端光标位置和服务器的 `cursor` 变量同步。

### 4.1 最简单方案——全行重绘

每次编辑都重绘整行：
```
1. 发 \r                    → 光标回到行首
2. 发 \x1b [ K              → 清除光标到行尾（擦掉旧内容）
3. 发 sql_buf              → 输出新的行内容
4. 发 \r + \x1b [ <n> C     → 光标移到正确位置
```

`\x1b [ K` 是 "Erase to End of Line"。

`\x1b [ <n> C` 是 "Cursor Forward n columns"（向右移 n 格）。

示例：`sql_buf = "SET key val"`, `cursor = 4`（光标在 `k` 上）
```
\r                      → 回到行首
\x1b [ K                 → 清除
SET key val              → 重绘
\r                      → 回到行首
\x1b [ 4 C               → 光标右移 4 格, 落在 'k' 处
```

### 4.2 增量移动（更高效但更复杂）

不重绘整行，只移动光标和局部更新：
- 左移：`\x1b [ D`（Cursor Back）
- 右移：`\x1b [ C`（Cursor Forward）
- 插入字符后：发 `\x1b [ @`（Insert Character）或重绘从 cursor 到行尾的部分
- 删除字符后：发 `\x1b [ P`（Delete Character）或重绘从 cursor 到行尾的部分

增量方式复杂很多，v0.1 用全行重绘足够。

## 5. 完整处理流程伪代码

```
int cursor = 0;
string sql_buf;
bool in_esc = false;
string esc_buf;

for (char c : received_data) {
    if (in_esc) {
        esc_buf += c;
        if (c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z') || c == '~' {
            // 序列完成
            if (esc_buf == "\x1b[D")       cursor = max(0, cursor - 1);
            else if (esc_buf == "\x1b[C")  cursor = min(sql_buf.size(), cursor + 1);
            else if (esc_buf == "\x1b[H")  cursor = 0;
            else if (esc_buf == "\x1b[F")  cursor = sql_buf.size();
            else if (esc_buf == "\x1b[3~") { if (cursor < sql_buf.size()) sql_buf.erase(cursor, 1); }
            in_esc = false;
            redraw();
        }
    }
    else if (c == '\x1b') {
        in_esc = true; esc_buf = "\x1b";
    }
    else if (c == '\b' || c == 0x7F) {
        if (cursor > 0) { sql_buf.erase(cursor-1, 1); cursor--; redraw(); }
    }
    else if (c == '\n') {
        // 去掉 \r, 执行命令, 输出结果, 清空 sql_buf, cursor=0
    }
    else if (c >= ' ') {
        sql_buf.insert(cursor, 1, c);
        cursor++;
        redraw();
    }
}
```

## 6. Windows telnet 注意事项

- Windows telnet 默认发送 `\b`（0x08）作退格，不是 DEL（0x7F），两个都处理最安全
- Windows telnet 发 Home/End 时，通常发 `\x1b [ H` / `\x1b [ F`
- 某些 Ctrl 组合键也会发控制字符：`Ctrl+A` → `\x01`, `Ctrl+E` → `\x05`, `Ctrl+D` → `\x04`，可选择性支持
- telnet 协商只影响初始连接阶段，方向键的 ESC 序列是之后的数据，`filter_telnet` 已经处理完就不会干扰

## 7. 进阶参考——GNU Readline 的快捷键

如果想进一步支持更多快捷键（不需要全实现）：

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+A` | 光标跳到行首（同 Home） |
| `Ctrl+E` | 光标跳到行尾（同 End） |
| `Ctrl+D` | 删除光标处字符（同 Delete），行空时断开连接 |
| `Ctrl+K` | 删除光标到行尾 |
| `Ctrl+U` | 删除光标到行首 |
| `Ctrl+W` | 删除前一个单词 |
