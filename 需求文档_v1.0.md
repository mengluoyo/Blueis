# Blueis - 非关系型缓存数据库 需求文档 v1.0

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v0.1 | - | 初稿，定义 Phase 1-5 基础功能 |
| v1.0 | 2026-05-28 | 标记 Phase 1-4 已完成，新增 Phase 5-11 生产级特性规划 |

---

## 1. 项目概述

### 1.1 项目背景

仿照 Redis 设计并实现一个轻量级的非关系型（NoSQL）内存数据库，命名为 **Blueis**。项目以**学习数据库底层原理**为核心目标，通过逐阶段实现生产级特性，深入理解存储引擎、协议解析、持久化、复制、集群等数据库核心技术。

### 1.2 核心目标

- 提供 Redis 风格的内存数据存储能力
- 支持使用 **SQL 语句** 和 **Redis 命令** 两种方式操作数据
- 逐步实现从单机到分布式的完整数据库特性栈
- 通过实战学习数据库底层系统编程

---

## 2. 技术选型

| 层面 | 当前方案 | 说明 |
|------|---------|------|
| 编程语言 | C++17 | Windows/MinGW，零运行时依赖 |
| 构建工具 | CMake | MinGW Makefiles |
| SQL 解析 | 手写递归下降解析器 | Lexer → Parser → AST → Executor |
| 网络协议 | 自定义文本协议 | telnet 兼容，后续升级为 RESP |
| 持久化 | 自建 JSON 解析器 | 快照方式，无外部依赖 |
| 并发 | `std::thread` + `std::shared_mutex` | 每连接一线程 |

---

## 3. 已完成功能（Phase 1-4）

### Phase 1 — TCP Server + String 命令

- [x] 项目初始化（CMake + C++17 工程搭建）
- [x] TCP Server 基础框架（Winsock2, 端口 6380）
- [x] 文本协议解析器（`CommandParser`, token 化 + SQL/Redis 路由）
- [x] Storage Engine 单例：String 类型的 SET/GET/DEL/EXISTS/KEYS
- [x] 多线程客户端处理（per-connection thread）
- [x] Telnet IAC 协商过滤

### Phase 2 — SQL 引擎

- [x] SQL 词法分析器（Lexer）：Keyword / Identifier / Integer / String / Operator
- [x] SQL 语法分析器（Parser）：CREATE TABLE / INSERT / SELECT / UPDATE / DELETE / DROP TABLE / SHOW TABLES
- [x] AST 定义（`std::variant`, 8 种语句类型）
- [x] WHERE 子句表达式树（`=`, `!=`, `>`, `<`, `>=`, `<=`, `LIKE`, `AND`, `OR`）
- [x] SQL Executor：AST → StorageEngine 操作
- [x] 结果格式化（ASCII 表格输出）

### Phase 3 — Hash 命令

- [x] Hash 类型：HSET / HGET / HDEL / HEXISTS / HGETALL / HKEYS
- [x] Redis 命令与 SQL 命令统一路由
- [x] 多行命令输入支持（分号结束）

### Phase 4 — 持久化

- [x] 自建 JSON Writer/Reader（无第三方依赖）
- [x] 快照保存（SAVE 命令, `data/blueis.json`）
- [x] 启动时自动加载快照
- [x] 服务关闭时自动保存
- [x] 终端行编辑支持（左右方向键、中间插入/删除、全行重绘）

---

## 4. 待实现功能（Phase 5-11）

### Phase 5 — RESP 协议兼容 ★★☆

**目标**：让标准 `redis-cli` 能直接连接 Blueis。

| 功能 | 说明 |
|------|------|
| RESP2 协议解析 | `+OK\r\n`, `-ERR\r\n`, `$bulk\r\n`, `*array\r\n`, `:integer\r\n` |
| RESP2 协议序列化 | 将执行结果编码为 RESP 格式 |
| 协议自动检测 | 根据首字节区分 telnet 文本 / RESP |
| `redis-cli` 兼容 | 不经任何修改即可连接使用 |

**学习收获**：二进制协议解析、网络协议设计、序列化/反序列化模式。

---

### Phase 6 — AOF 持久化 ★★★

**目标**：实现数据零丢失（或秒级 RPO），替代当前的全量 JSON 快照。

| 功能 | 说明 |
|------|------|
| AOF 日志追加 | 每条写命令追加到 `blueis.aof` 文件 |
| AOF 重放恢复 | 启动时顺序执行 AOF 中所有命令 |
| AOF Rewrite | 定期将 AOF 压缩为当前数据的最小命令集 |
| `BGREWRITEAOF` 命令 | 手动触发 rewrite |
| 混合持久化 | RDB 快照 + AOF 增量 |

**学习收获**：WAL（Write-Ahead Log）模式、文件追加写优化、命令重放、后台 rewrite 算法。

---

### Phase 7 — EXPIRE 过期机制 ★★

**目标**：支持 key 自动过期，实现内存生命周期管理。

| 功能 | 说明 |
|------|------|
| `EXPIRE key seconds` | 设置过期时间（秒） |
| `EXPIREAT key timestamp` | 设置过期时间（Unix 时间戳） |
| `TTL key` | 查看剩余生存时间 |
| `PERSIST key` | 移除过期时间 |
| 惰性删除 | 访问 key 时检查是否过期 |
| 定期删除 | 后台定时抽样清理过期 key |

**学习收获**：定时器设计、惰性 vs 主动删除策略权衡、内存压力管理。

---

### Phase 8 — 内存管理 ★★

**目标**：防止内存无限增长，支持内存上限和淘汰策略。

| 功能 | 说明 |
|------|------|
| `maxmemory` 配置 | 设置最大内存上限 |
| LRU 淘汰 | 最近最少使用 |
| LFU 淘汰 | 最不经常使用 |
| TTL 淘汰 | 优先淘汰即将过期的 key |
| `INFO memory` | 内存使用统计 |

**学习收获**：LRU/LFU 算法实现、近似计数、内存统计与监控。

---

### Phase 9 — IO 多路复用 ★★★★

**目标**：单线程处理数千并发连接，告别 per-connection thread 模型。

| 功能 | 说明 |
|------|------|
| Event Loop | 单线程事件循环 |
| `select` / `epoll` | Windows 上用 `select`，后续考虑 IOCP |
| 非阻塞 IO | 所有 socket 设为非阻塞 |
| 连接池管理 | 统一管理所有客户端连接 |

**学习收获**：事件驱动编程、IO 多路复用原理、单线程高并发模型、React 模式。

---

### Phase 10 — 主从复制 ★★★★

**目标**：实现数据冗余和读写分离。

| 功能 | 说明 |
|------|------|
| `REPLICAOF` / `SLAVEOF` | 建立主从关系 |
| 全量同步 | RDB 快照 + 增量命令 |
| 部分重同步 | `repl_backlog` 环形缓冲区 |
| `PSYNC` 命令 | 部分同步协议 |
| 复制偏移量跟踪 | `repl_offset` |

**学习收获**：分布式一致性基础、binlog/replication log、环形缓冲区、断线重连。

---

### Phase 11 — 哨兵与集群 ★★★★★

**目标**：实现高可用自动故障转移和数据分片。

| 功能 | 说明 |
|------|------|
| Sentinel 哨兵 | 监控主节点、自动故障转移、通知客户端 |
| 主观下线（SDOWN） | 单个哨兵判定节点不可达 |
| 客观下线（ODOWN） | 多数哨兵达成共识 |
| 领导者选举 | Raft 简化版 |
| Cluster 分片 | 哈希槽（16384 slots）、请求重定向 |
| `MOVED` / `ASK` | 分片重定向响应 |

**学习收获**：分布式共识算法（Raft）、故障检测、选举、数据分片、一致性哈希。

---

## 5. 路线图总览

```
已完成              基础进阶                高可用                 分布式
Phase 1-4    →    Phase 5-8          →    Phase 9-10        →    Phase 11
───────           ───────────              ──────────             ──────────
TCP Server        RESP 协议               IO 多路复用            哨兵/集群
SQL 引擎          AOF 持久化              主从复制
Hash 命令         过期机制
JSON 持久化       内存管理
```

---

## 6. 当前系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    Client                                │
│     (telnet / 自定义客户端 / 未来: redis-cli)             │
└────────────────────┬────────────────────────────────────┘
                     │ TCP (6380)
                     ▼
┌─────────────────────────────────────────────────────────┐
│                Server (Winsock2, 多线程)                  │
│  ┌─────────────────┐  ┌──────────────────────────────┐  │
│  │ Protocol Parser  │  │  Client Handler (per thread) │  │
│  │ (SQL / Redis)    │  │  (行编辑 / IAC 过滤)          │  │
│  └────────┬────────┘  └──────────────────────────────┘  │
└───────────┼──────────────────────────────────────────────┘
            ▼
┌─────────────────────────────────────────────────────────┐
│                   SQL Engine                             │
│  Lexer ──→ Parser (递归下降) ──→ AST ──→ Executor        │
│                                              │           │
│                    Redis Engine ◄────────────┘           │
│                    (SET/GET/HSET/...)                    │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│                Storage Engine (单例)                      │
│  m_strings  │  m_hashes  │  m_tables  │  m_rows          │
│  (shared_mutex 线程安全)                                  │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Persistence Layer                            │
│  Snapshot (JSON)  +  未来: AOF                            │
│  data/blueis.json                                        │
└─────────────────────────────────────────────────────────┘
```

---

## 7. 模块职责

| 模块 | 职责 | 状态 |
|------|------|------|
| **Server** | TCP 监听、连接管理、多线程调度、行编辑 | ✅ 完成 |
| **Protocol Parser** | 解析文本命令，区分 SQL / Redis 命令 | ✅ 完成 |
| **SQL Engine** | Lexer → Parser → AST → Executor → Formatter | ✅ 完成 |
| **Redis Engine** | SET/GET/DEL/EXISTS/KEYS + HSET/HGET/HDEL/HEXISTS/HGETALL/HKEYS | ✅ 完成 |
| **Storage Engine** | 内存数据管理（String + Hash + Table/Row），`shared_mutex` 线程安全 | ✅ 完成 |
| **Persistence** | JSON 快照保存/恢复，自建解析器 | ✅ 完成 |
| **RESP Protocol** | RESP2 协议解析与序列化，redis-cli 兼容 | 📋 待实现 |
| **AOF** | 命令日志追加、重放、rewrite | 📋 待实现 |
| **Expire** | Key 过期与淘汰策略 | 📋 待实现 |
| **Replication** | 主从同步、复制偏移量、部分重同步 | 📋 待实现 |
| **Cluster** | 哨兵、故障转移、哈希槽分片 | 📋 待实现 |

---

*本文档为 v1.0 版本，Phase 1-4 已完成，Phase 5-11 为后续规划。*
